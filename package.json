{
  "name": "chrome-extension",
  "private": true,
  "description": "Publish chrome extension to the Chrome Web Store.",
  "scripts": {
    "build": "tsc && ncc build dist/index.js && cp action.yml dist",
    "clean": "rm -rf dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wdzeng/chrome-extension"
  },
  "author": "Hyperbola",
  "dependencies": {
    "@actions/core": "^1.8.0",
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "@types/node": "^16.4.1",
    "@vercel/ncc": "^0.33.4",
    "typescript": "^4.6.4"
  }
}
