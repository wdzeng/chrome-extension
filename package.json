{
  "name": "chrome-extension",
  "description": "Publish extension to Chrome Web Store.",
  "version": "1.3.2",
  "author": "hyperbola",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wdzeng/chrome-extension"
  },
  "license": "MIT",
  "imports": {
    "#/*": "./src/*.ts"
  },
  "scripts": {
    "build": "pnpm clean && pnpm transpile && cp action.yml dist",
    "transpile": "esbuild src/index.ts --platform=node --target=node24 --minify --bundle --outfile=dist/index.cjs",
    "test": "pnpm clean && tsx -r dotenv/config test/index.ts dotenv_config_path=test/.env.local",
    "transpile:test": "esbuild test/index.ts --platform=node --target=node24 --minify --bundle --outfile=dist/test/index.cjs",
    "lint": "eslint --fix src",
    "renew-credentials": "pnpm clean && tsx -r dotenv/config test/renew-credentials.ts dotenv_config_path=test/.env.local",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "axios": "^1.17.0",
    "glob": "^13.0.6"
  },
  "devDependencies": {
    "@types/node": "^24.13.1",
    "dotenv": "^17.4.2",
    "esbuild": "^0.28.1",
    "eslint": "^10.5.0",
    "eslint-config-wdzeng": "github:wdzeng/eslint-config#semver:^0.16.0",
    "prettier": "^3.8.4",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=24"
  },
  "devEngines": {
    "node": ">=24"
  }
}
